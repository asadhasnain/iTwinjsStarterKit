

import * as dotenv from "dotenv";
// Load environment variables from .env file
dotenv.config();

/**
 * Main function initializing logging
 * @async
 * @function main
 * @returns {Promise<void>}
 */
(() => {

  console.info("Clean Boiler plate to start iTwin.js node project");
  
})();